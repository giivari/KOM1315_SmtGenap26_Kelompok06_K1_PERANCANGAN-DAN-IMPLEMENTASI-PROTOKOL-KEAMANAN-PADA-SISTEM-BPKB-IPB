node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# SQLite dev database (no longer used — now using PostgreSQL)
prisma/dev.db
prisma/dev.db-journal

# Uploaded files (jangan commit ke repo)
uploads/
